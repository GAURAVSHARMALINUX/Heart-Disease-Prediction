name: Sync Wiki (Disabled - Duplicate)

# This workflow is disabled. Use update-wiki.yml instead.
# Both workflows were doing the same job but watching different folders.
# update-wiki.yml watches wiki_content/ which is the correct folder.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use update-wiki.yml'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Use update-wiki.yml instead."
